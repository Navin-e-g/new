import React from "react"
function Day3Task3(){
    return(
        <div>
            <h3>the default in <b>"export default function"</b> simplifies the import process for the developer, as there's no need to remember the exact name of the exported component,only the file path is necessary.</h3>
        </div>
    )
}
export default Day3Task3;